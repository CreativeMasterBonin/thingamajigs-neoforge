package net.rk.thingamajigs.mix;

/*@Debug(export = true)
@Mixin(value = ItemBlockRenderTypes.class)*/
public class TChunkRenderTypes {
    /*@Shadow private static boolean renderCutout;

    @Inject(method = "getChunkRenderType",at={@At(value = "RETURN")},cancellable = true)
    private static void getChunkRenderTypeThingamajigs(BlockState state, CallbackInfoReturnable<RenderType> cir){
        if(state.getBlock() instanceof CustomLeavesBlock){
            cir.setReturnValue(renderCutout ? RenderType.cutoutMipped() : RenderType.solid());
        }
    }

    @Inject(method = "getMovingBlockRenderType",at={@At(value = "RETURN")},cancellable = true)
    private static void getMovingBlockRenderTypeThingamajigs(BlockState state, CallbackInfoReturnable<RenderType> cir){
        if(state.getBlock() instanceof CustomLeavesBlock){
            cir.setReturnValue(renderCutout ? RenderType.cutoutMipped() : RenderType.solid());
        }
    }*/
}
